<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📂 Personal Autorizado</h2> <!-- Título serio -->
        <a href="/registro" class="btn btn-dark">+ Nuevo Empleado</a>
    </div>

    <div class="list-group shadow-sm">
        {{#each visitantes}}
            <div class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">{{this.nombre}}</h5>
                    <small class="text-muted">{{this.fecha_formateada}}</small>
                </div>
                <p class="mb-1 text-muted">{{this.correo}}</p>
                <small class="text-secondary"> <!-- Color más discreto -->
                    <i class="bi bi-shield-lock"></i> Dispositivo: {{this.datos_tecnicos}}
                </small>
            </div>
        {{else}}
            <div class="alert alert-info text-center">
                No hay nadie registrado aún. ¡Sé el primero!
            </div>
        {{/each}}
    </div>

    <div class="text-center mt-5">
        <a href="/" class="btn btn-secondary">Volver al Inventario</a>
    </div>
</div>